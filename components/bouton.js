import React from "react";
import { View, Text } from 'react-native';

function Bouton({title}) {
    return (
        <view>
            <Bouton title={title}></Bouton>
        </view>
    )
}
export default Bouton